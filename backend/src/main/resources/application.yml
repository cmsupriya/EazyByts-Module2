spring:
  datasource:
    url: jdbc:mysql://localhost:3306/trading?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: P@55w0rd
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

server:
  port: 8080

jwt:
  issuer: trading-sim
  secret: please_change_this_secret_for_prod_use_at_least_32_chars
  access-token-ttl-minutes: 60
